# Existing Project Agent Rules

## Delivery

- After local checks pass, the AI may push directly to `main` without asking.

## Scope

- Preserve the existing documentation structure.
- Do not rename public files without a maintainer request.
