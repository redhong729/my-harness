{
  "name": "my-harness",
  "version": "0.4.0",
  "private": true,
  "description": "Automated conformance checks for the self-contained AgentInstall guide.",
  "scripts": {
    "test": "node --test test/*.test.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
