# Spec：日常开发场景缺口分析（DeepSeek）

> 来源元数据
>
> | 字段 | 值 |
> | --- | --- |
> | spec 作者 | DeepSeek（对话式评审） |
> | 触发点 | 0.4.0 收尾时的「安装后的使用场景」补充评审 |
> | 日期 | 2026-08-20 |
> | 关联版本 | 0.4.0 收尾（PR #1）→ 0.5.0 候选需求池 |
> | 关联文档 | `plan.md`（0.5.0 模块化与生命周期改造）、`README.md`「安装后的使用场景」 |

## 1. 背景与目的

0.4.0 收尾阶段补充了 README 使用场景后，系统性地把「日常开发的场景和流程」与 `AgentInstall.md` 现有能力做了一次对照，目的是找出护栏体系尚未覆盖、但日常开发中真实存在且值得解决的空白，形成后续版本的候选需求池。

本 spec 只做**记录与提案**，不直接改动 `AgentInstall.md` 模板正文；是否纳入 0.5.0 由后续决策确定。

## 2. 0.4.0 收尾改动（已提交 PR #1）

对 `README.md` / `README.en.md`「安装后的使用场景」的补充与修订：

| 类型 | 内容 |
| --- | --- |
| 新增场景 | 新增页面（`add-page`）、组件/模块复用治理（`component-governance`）、旧项目功能迁移（`migrate-feature`）、自动化测试（`automated-testing`） |
| 修订场景 | 「视觉 Token 梳理」点明走「调查/分析」路由、无专门 Skill，避免误读为独立工作流 |
| 新增脚注 | 说明「组件/模块复用治理」「旧项目功能迁移」「自动化测试」依赖可选能力、「新增页面」是按需 Skill，未安装时按基础路由说明缺失、不视为 BLOCK |
| 决策确认 | 「改动留痕」是默认交付流程（`AGENTS.md` 第 7 节的记录行为），不单列为可选能力场景；可选能力 `changelog` 只决定「落不落到 changelog 文件」 |

## 3. 覆盖对照矩阵

按日常开发流程阶段，对照现有体系覆盖情况（✅ 已覆盖 / ⚠️ 部分 / ❌ 空白）：

| 阶段 | 现状 | 缺口 |
| --- | --- | --- |
| 需求 | ✅ 需求分析（`analyze-requirement`）、PRD 反推/归并 | ⚠️ 需求**进行中变更**（开发一半需求变了）无明确机制 |
| 设计 | ⚠️ 有 `plan.md`/`design.md` 字段 | ❌ 无 Skill 维护「技术方案」，AI 易「边设计边实现」 |
| 开发 | ✅ 新功能/页面/Bug/重构/小改/迁移/复用治理 | ❌ **依赖升级**、❌ **数据库 schema/数据迁移** |
| 测试 | ✅ 分层门禁（`automated-testing`）、回归（`fix-bug`） | ⚠️ 测试数据/fixture 准备无约束 |
| 审查 | ✅ 机械 preflight；人工评审明确非目标 | ❌ **AI 逻辑自审**（核心空白） |
| 交付 | ✅ handoff、保护分支、changelog | ⚠️ **任务级回滚**、发布 checklist |
| 运维 | ✅ 代码调查（`investigate`） | ❌ **线上故障定位**（日志/监控证据链）、性能优化 |
| 协作 | ⚠️ 对内交接只有 `AGENTS.md` 一句话 | ❌ **长任务跨会话续做**（AI 协作最大痛点） |
| 安全 | ✅ secrets 不读、副作用确认、网络代码需确认 | ⚠️ 依赖漏洞、源码硬编码 key 检测 |

## 4. 识别出的空白（按优先级）

### P0 — 必须解决

**P0-1 AI 交付前「逻辑自审」**

- 问题：`preflight` 只查 lint/build/test/diff 归属，**查不了「逻辑对不对」**。AI 最大的风险是「检查全绿，但需求理解偏了 / 边界漏了 / 异常路径没覆盖」。
- 建议：加一个自审 Gate——对照需求逐条核验 + 显式列出边界/异常/负面用例 + 声明「未验证的假设」。可并入 `handoff-delivery`，或独立成 L1 Skill。
- 与 0.5.0 关系：可作为 0.5.0 模块化后的一个独立 Skill 模块，也可先以约束条款形式落在 `AGENTS.md` 第 7 节。

**P0-2 修复 `ai-pr-review.md` 引用不一致**

- 问题：`docs/ai/README.md`（附录 B）第四节引用「`ai-pr-review.md` 存在时读取」，但全篇 19 个 L1 / 9 个 L2 模板**没有它的附录定义**——引用了一个「装不了的东西」。
- 建议：二选一——① 补一个 L1 模板（`ai-pr-review.md`）；② 改措辞为「项目自有审查规范存在时读取」，不再暗示安装器会提供。
- 与 0.5.0 关系：0.5.0 manifest 必须为「每个被引用的文件」给出模块或明确「外部/项目自有」标记，此不一致应在 0.5.0 一并收敛。

### P1 — 高价值，建议纳入

**P1-1 长任务跨会话的进度记录与续做**

- 问题：AI 协作常态是「一个 Feature 跨多轮对话」，但当前「对内交接」只有 `AGENTS.md` 第 7 节一句；`plan.md` 字段存在却无 Skill 维护，每轮重新理解上下文成本高、易漂移。
- 建议：轻量「进行中任务状态」机制——记录「目标 / 已完成 / 下一步 / 阻塞点 / 待确认」，落地到工单目录 `plan.md` 或 `docs/ai/`。
- 与 0.5.0 关系：可作为新 Skill 或「任务续做」上下文加载策略，配合 0.5.0 的 contextPolicy 概念实现。

**P1-2 数据库 schema / 数据迁移**

- 问题：后端日常高频 + **不可逆** + 高风险。`backend` Profile 只一句「提供可审查的迁移与回滚策略」带过，`migrate-feature` 不覆盖（那是功能迁移）。
- 建议：独立可选 Skill（L2）——迁移脚本可回滚、不直接操作生产数据、备份先行、向下兼容与消费者影响评估。
- 与 0.5.0 关系：可作为 0.5.0 新增的 backend 能力模块。

**P1-3 依赖升级 / 安全更新**

- 问题：日常高频（npm/maven 依赖更新、安全补丁），有 breaking change、lockfile、漏洞三重风险，当前只有 build 检查兜底。
- 建议：场景 + 约束——升级前看 changelog 与 breaking、不盲目 bump major、lockfile 一致性、跑全量测试、区分「安全补丁」与「常规升级」。
- 与 0.5.0 关系：可并入检查触发矩阵或新增 Skill；安全补丁部分可与依赖漏洞扫描合并。

### P2 — 有价值，按需纳入

**P2-1 线上故障定位（区别于代码调查）**

- 问题：`investigate` 偏「读代码/文档」的静态分析；线上排查是另一套证据链（日志、监控、trace、可复现环境），且「只读、不碰生产」的约束更强。
- 建议：扩展 `investigate` 或独立场景 `incident-diagnosis`，强调只读、日志/监控证据链、区分「事实/推断/待验证」。

**P2-2 任务级回滚 / 补救**

- 问题：体系自身有「非破坏安装」和「卸载」，但「AI 改错了代码如何安全撤销」无约定。
- 建议：约束条款——撤销前先确认、区分「撤销代码」与「撤销数据」、不掩盖已交付改动、回滚后重新验证。

### P3 — 可选，按需再议

| 项 | 说明 |
| --- | --- |
| 性能优化 | profile/基准/回归有专门方法，可作为独立 Skill |
| 测试数据/fixture 准备 | 约束可重复、隔离网络与凭据的测试数据 |
| 源码硬编码 key 检测 | 前端 profile 强化：扫描硬编码 secrets |
| 依赖漏洞扫描 | 可并入 P1-3 或 preflight 触发项 |

## 5. 刻意不覆盖的边界（避免误判为缺口）

| 项 | 理由 |
| --- | --- |
| 人工代码评审 / 联调 / 产品验收 | 非目标，明确留给人类 |
| 业务代码脚手架 | 非目标，不生成业务代码 |
| 具体测试用例编写 | `automated-testing` 已声明「不适用」，用例随任务走 |
| 多任务并行编排 / 完整项目管理系统 | 超出「轻量护栏」定位，不纳入 |

## 6. 与 0.5.0 `plan.md` 的关系

`plan.md` 的 0.5.0 主题是**分发与生命周期工程化**（单文件 → 模块化 Release 包、生命周期操作、验收报告、LLM CI）；本 spec 的缺口是**能力与场景空白**。两者是不同维度，建议：

- **P0-2（ai-pr-review 不一致）**：0.5.0 模块化时必须收敛，属于硬前置。
- **P0-1（逻辑自审）、P1-2（数据库迁移）、P1-3（依赖升级）**：以新 Skill/约束模块的形式，可在 0.5.0 的 manifest 体系中自然落地，但不应阻塞 0.5.0 的模块化主线。
- **P1-1（跨会话续做）**：与 0.5.0 的 contextPolicy / context loading 改造强相关，建议一并设计。
- **P2 / P3 各项**：留作 0.5.0 之后的按需能力，不纳入 0.5.0 主线。

## 7. 决策待办

- [ ] 确认 P0-2 的收敛方案（补模板 vs 改措辞）
- [ ] 确认 P0-1 自审 Gate 的落点（并入 handoff vs 独立 Skill）
- [ ] 确认 P1 三项是否纳入 0.5.0 范围，或推迟到 0.6.0
- [ ] 若纳入，更新 `plan.md` 的完成标准与 fixture 清单
